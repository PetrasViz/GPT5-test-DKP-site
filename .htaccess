# Redirect everything from root to /public
RewriteEngine On

# Don’t apply redirect if already inside /public
RewriteCond %{REQUEST_URI} !^/public/

# Redirect all requests to /public while keeping the URI
RewriteRule ^(.*)$ /public/$1 [L]
